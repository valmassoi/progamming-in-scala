//scala listings-3.4.scala
//tuples can contain different types unlike lists

val pair = (99, "Luftballons") // of type Tuple2[Int, String]
println(pair._1) // one-based index (because Haskell/ML)

// defines them up to Tuple22